extends layouts/main.pug

block content
    .video__player
        video(src=`/${video.fileUrl}`)
    .video__info
        a(href=routes.editVideo(video.id)) Edit_Video
        h5.video__title=video.title
        span.video__views=video.views
        p.video_description=video.description